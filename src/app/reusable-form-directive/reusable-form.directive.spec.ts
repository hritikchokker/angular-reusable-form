import { ReusableFormDirective } from './reusable-form.directive';

describe('ReusableFormDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new ReusableFormDirective();
  //   expect(directive).toBeTruthy();
  // });
});
